import styled from 'styled-components'
import { cores } from '../../styles'

export const Container = styled.div`
  padding-top: 24px;
  padding-bottom: 32px;
  height: 280px;
  color: ${cores.branco};
  font-size: 32px;
  line-height: 38px;

  p {
    margin-bottom: 156px;
    font-weight: 100;
  }

  h2 {
    font-weight: 900;
  }
`
